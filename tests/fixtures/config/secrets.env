# CI fixture — never real credentials. Matches the compose "minio" service's
# MINIO_ROOT_USER / MINIO_ROOT_PASSWORD.
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
RESTIC_PASSWORD=ci-testpass
