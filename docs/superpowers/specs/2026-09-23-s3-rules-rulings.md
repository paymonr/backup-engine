# S3 rules — decision log (controller rulings)

Every ruling made while building the S3 rules feature (spec `2026-09-23-s3-rules-design.md`, plan
`plans/2026-09-23-s3-rules.md`), in order. Format: *what was decided — why — what it costs if wrong*.
Exported from the gitignored SDD ledger on 2026-09-24 so it survives a move to another machine.

- spec §1 says jobs_io validates count 1..100 for Plain copy, plan clamps in lifecycle.plain_rule instead — keep the clamp for Phase A (a load-path validation would silently drop existing jobs, incl. restic count>100); Phase B's wizard enforces ≤100 for Plain copy with a form error — cost if wrong: a hand-edited archive count >100 keeps only 100 (S3 max) without a warning until Phase B.
- treat Task 2 minor (1) as a real gap and run fix round 1 — spec §6 makes feature "s3-rules" gate the applies, so Verify stamping level 4 without proving BaseBucketRules would enable applies that then AccessDenied — add a positive role probe `get-bucket-lifecycle-configuration --bucket <base>` (ok on rc 0 or NoSuchLifecycleConfiguration); fold in minors (2),(3),(4) — cost if wrong: one more probe round-trip on Verify.
- Task 3 minor "two jobs can point at the same dedicated bucket → duplicate backup-engine:bucket rule IDs (S3 rejects) and conflicting whole-bucket rules" is real → carry into Task 7: job_save refuses a new dedicated job whose bucket is already used by another job (guided message, before any AWS call) — cost if wrong: one extra validation.
- accept Task 5 implementer's two deviations — (a) ordering assert split into separate [ ] lines because `A && B && C` under set -e swallowed failures (my ruling's form was wrong); (b) no-op python3 stub in the order test so the pre-Task-6 archive_prune doesn't hit real AWS — cost if wrong: none (Task 6 removes archive_prune; stub pattern already used in the file).
- ONE fix wave now covering I1, I2, I3, the check/check_all never-raise + malformed-job safety (#4), atomic + locked state (#5), wizard Plain copy count>100 form error (#6), a timeout on the check in backup-job.sh (#10), cosmetics (#11: trigger param, ${S3_BUCKET:-}, friendlier CLI wording), dead code + stale docs (#12: s3.delete_version, iam-version-perms copy, README.md + opentofu/README.md), tests (#14: never-raise, dedicated-bucket check) — the reviewer allowed I1 in Phase B but it's security and contained, so it goes now — cost if wrong: a larger fix diff to review.
- defer to Phase B (must land before deploy): #7 seed undo_days from a legacy backstop >30 days (the owner's box is exactly 30; the Phase B preview/keeps-less gate is the right home); #13 Verify probe proving the runtime key lost DeleteObjectVersion; an hourly tamper check independent of backups — cost if wrong: a custom-backstop install loses undo days if deployed before Phase B.
- park #8 (pre-existing jobs sharing one dedicated bucket) — no dedicated-bucket job exists on any install yet (the feature only just became usable) and Task 7 blocks new ones — cost if wrong: that bucket's rules stay in `error` until one job is removed.
- park #9 (transition minimum object size) to Phase C with the tier work — cost if wrong: none until transitions exist.
- Phase A closed at df721bc. Residuals go into a first Phase B task "Phase A carry-overs" (one batched dispatch): M1 (same bucket: a restored alarm replaces an earlier not_restored, console rule IDs kept), M2 (status/alarm first, then fingerprint), M3 (an OpenTofu `removed` block that FORGETS aws_s3_bucket_lifecycle_configuration.backup instead of destroying it — console rules survive a re-run — plus README; required_version bumped to what the syntax needs), M4, O2 (acknowledge never 500s; acknowledges only alarms up to the one the owner saw), O4 (check CLI --trigger from BE_TRIGGER) — nothing deploys between phases, so parking them in B costs nothing — cost if wrong: none.
- O1 → Phase B gate task (first apply lists existing console rules that can delete/move backups in its Activity entry + the S3 rules screen overlap flag); O3 → resolved by the Phase B gate (a shorter undo window from unreadable settings becomes a pending keeps-less change, never applied) — cost if wrong: an owner's hand-set longer undo window could shrink before Phase B lands (no UI sets it in Phase A).
- on a FIRST apply (no applied doc) a folder is known only if at least one of its jobs has run (state/<job>.json exists in the cache — it may have data in S3); folders whose jobs never ran are new → keeps-more → applied at once. Implement as `baseline_from_live(live_rules, want, known=None)` with the caller computing known from the cache (None → want.folders, the careful default); outstanding() uses the same. Carry into Task 11 AND Task 16 (which restates baseline_from_live). — why: the plan as written makes a fresh scripted/guided install's FIRST job rule wait for confirmation (no rules yet → expiry added to a known folder = keeps-less); the owner's box (jobs that have run) still goes through the gate — cost if wrong: a lost cache on an existing install makes folders look new (edge accepted).
- accept planner judgment calls 1–14 (notably #2 keeps-less by exact deletion sets: adding newest-N at the same days keeps more under S3 semantics; the spec's "added" case = days-only → count form, which lowers days and stays keeps-less) — cost if wrong: a keeps-less change classed keeps-more would apply without preview; the parametrized classify table covers every spec case.
- storage summary after successful runs only (open Q) — the next success or Refresh now covers it — cost if wrong: previews after a failed-but-partial run use an older summary (stamped "as of").
- deploy only after Phase C is reviewed and the Task 19 real-AWS smoke test passes (recommendation; deploy itself needs the owner).
- keep the bare `removed { from = aws_s3_bucket_lifecycle_configuration.backup }` (no lifecycle block) — OpenTofu docs: a removed block without lifecycle forgets (not destroys); the explicit `lifecycle { destroy = false }` form needs a newer tofu than the container's OPENTOFU_VERSION=1.8.5 (Dockerfile:26); the validate warning is advisory — cost if wrong: none (both forms forget).
- park Minors 1–4 for the final Phase B/C fix wave (small, no behavior risk); 5 and 6 accepted as-is (5 errs toward alarm; 6 needs a disk failure between two local writes) — cost if wrong: a cosmetic log-wording drift / one misleading flash.
- I1 → never forget a known folder: applied.folders = old ∪ want (a re-created job's folder is known → keeps-less waits) — supersedes R-B2's accepted edge — cost if wrong: an owner re-creating a job under an old name confirms once.
- I2 → drop the seed_new_bucket call from job_save (R-B2′ already makes a brand-new bucket's never-run folder new); keep the function (later tasks' tests use it as a fixture helper) — cost if wrong: none.
- I3 + Minors fixed in round 1: the missing test + `_ran(...)` in the two hollow tests; _stricter keeps min/min only when one rule's deletions contain the other's, else keeps one rule unchanged (under-approximates what S3 deletes today → never hides keeps-less); "has run" = state/<job>.json OR state/<job>.runs.jsonl; a settings lock around storage.json read-modify-write (`settings_lock(config_dir)`, to be used by every later writer); one NCE parse helper; waiting line once. Parked: "X → X" wording (Phase C, 18a), module split (final review decides; later tasks' code references lc.* names).
- fix round 1 = Critical (`9>&-` on the detached launch + bats regression with a sleeping stub asserting the job lock is free after the run; also audit every other background launch in backup-job.sh/lib for inherited fd 9) + Minors 1, 2, 3, 4, 6. Accept 5 (needs create/delete/re-create in one second) and 7 (spec 7.7.3 design) — cost if wrong: a rare misattributed successor; a queued billing check.
- I1 → the pass itself enforces the confirmed target: `_reconcile_locked(..., gated=False, expect=<target_hash>)` compares _set_hash(want) right after reading the files; mismatch → write the GATED target and report stale (owner words); plus `edited()` normalizes a job edit exactly as jobs_io.upsert's validate will (root fix for (b)) — cost if wrong: an owner whose job changed mid-confirm confirms again.
- I2 → SUMMARY_STALE_DAYS = 7: a summary older than 7 days (or unparseable scanned_at) counts as missing for needs_typed — daily jobs refresh daily; a weekly job's owner may type the name more often — cost if wrong: an extra typed confirmation.
- fix round 1 also takes: settings_lock held across check→save (no re-entrant flock on the same file — inner save without the lock); token re-read inside the bucket lock (strict single use); typed must be a str; kind↔payload enforced; preview rejects a bucket not in buckets_for; own/waiting split — Preview gains `own: list[Change]`; token iff own keeps-less (job/settings edits) or anything waiting (confirm); `keeps_less`/`changes` keep meaning "everything the ungated write makes" (Task 15's route saves + gated-syncs when token is None, so waiting items stay waiting); applied rules hashed into inputs_hash (ABA); token kept on `invalid` documented + tested; the listed test gaps. Accept: needs_typed fixed for the token's 1 h life. Park: preview-section module split (final review).
- fix round 2 = all of the above: relative scanned_at (+ grep the suite for other date-dependent summary/freshness fixtures); save_edit validates kind↔payload; a no-AWS assertion that can fail (monkeypatch lc.role_creds/lc.read_rules/lc.write_rules to raise, or a raising aws on PATH); target_hash-only stale test (tamper the token's target_hash); lock-held-during-save assertion; `own` = keeps-less changes where the edit alters that rule vs the current want (not only rules absent from the waiting set). — cost if wrong: none.
- park the Minors (final review; versioning wiring lands in Task 16) — cost if wrong: none.
- fix round 1 = I1 (server-render the first impact line via impact_line from editor()'s values), I2 (flash + redirect when the editor is gone), and Minors: conditional max=100 (job-form pattern) + days-0 display, storage_summary.load validates the summary shape (malformed → treated as missing, never 500), honest flash for no-op/waiting re-saves, Refresh-now wording follows refresh_ok, MAX_DAYS = 36500 upper bound (form error), form errors keep the owner's entries, focusing/typing a number selects its radio, reuse lifecycle's _days and share the refresh check with the route, the listed test gaps. Park: _human_bytes vs routes._humanbytes (import direction; final review) — cost if wrong: none.
- fix round 2 = disable the number inputs of non-selected choices (JS on radio change + on load; the server ignores inactive fields) so an inactive field can never block submit; jsdom (or equivalent) test that checkValidity() is true after switching choices — the owner has hit this class of bug before (memory: silent form-submit block) — cost if wrong: none.
- fix round 3 = the server is the only gate for the editor's number boxes: remove client-side range constraints (min/max) and use step="any" so no native constraint validation can block submit (server already enforces 1–100, 1–MAX_DAYS, whole numbers in owner words); remove the server-rendered `disabled` and the disable/enable sync JS; keep "typing in a box selects its radio" (live again once fields are always enabled). Tests: markup has no min/max/disabled on those inputs and step="any"; the reviewer's scenario (keep=days&days=0 error → submit keep=count&count=20) previews in ONE POST; non-whole values (1.5) are a server form error. — cost if wrong: owners lose the browser's native range bubble (server message instead).
- fix round 1 = I1 (a half counts as tampered only when THAT half is out of step with target; after a successful write_rules, record the written rules with the old applied versioning before re-raising, so applied.json matches S3), I2 (bucket-wide no-token branch considers HOUSEKEEPING_ID and "versioning"), and Minors: write_versioning raises on an unexpected state; NotImplemented on versioning → versioning half unmanaged (rules keep working, owner-words note) instead of the whole bucket unsupported; opentofu `aws_s3_bucket_versioning` gets `lifecycle { ignore_changes = [versioning_configuration] }` (tofu creates it Enabled, the app owns it after — one owner per setting) + README; chip neutral when suspended by the owner's choice, wording consistent with change lines, hide "Suspend versioning…" while a suspend waits; one VERSIONING_STATES constant; LIFECYCLE_TIMEOUT default 180 (5 calls × connect 10 + read 30 worst case); the listed test gaps. Park: module split (final review). — cost if wrong: none.
- accept the implementer's call — a confirmed suspend whose versioning put failed is NOT retried ungated by a later unattended check; it becomes a normal waiting item needing a fresh confirmation (R-B4) — cost if wrong: the owner confirms the suspend twice after an AWS hiccup.
- fix round 2 = (a) the per-half tamper rule exactly as specified; (b) a write journal: before the puts, record the target about to be written as `in_flight` in applied.json (atomic); save_applied clears it; the next pass adopts in_flight as applied when live matches it (no alarm, no revert) — fixes kills/timeouts between any write and its record, incl. the confirmed-apply case; (c) the partial path records an Activity entry (rules applied; versioning couldn't be changed) and keeps the O1 first-apply note; (d) the alarm kind reflects whether the tampered half was restored (restored rules + failed untampered versioning put ≠ not_restored); (e) the radio assertion; (f) LIFECYCLE_TIMEOUT 240 with the comment's math corrected. — cost if wrong: one more field in applied.json.
- accept (d)'s approach — the alarm still requires acknowledgment, only its kind heals once a later pass is settled (answers Task 10 review Minor 5 too) — cost if wrong: an alarm reads "restored" after the owner fixed rules by hand.
- fix round 3 (last resume round) = journal in its own file `state/lifecycle/<bucket>.inflight.json` (never creates or rewrites applied.json; applied-absent stays first-apply), with a 6-hour TTL (older → dropped, judged normally; the hourly check-all resolves real crashes well within it); carry the journal through the error-path saves; heal on "ok" only when the versioning half is managed; the seeded O1 note on the failed-put path; tampered-branch Activity names what was restored vs not; and "status first" for tamper: on a tampered pass write the alarm BEFORE the puts, then update its kind after — cost if wrong: one more small state file per bucket.
- fix round 4 (fresh implementer, opus — R≥4 escalation): the two tests (mutation-proof: TTL disabled → fails; pre-write removed → fails); delete the journal only AFTER the adoption is saved; keep the journal on the rules-landed/versioning-errored branch (a put may have landed — the next pass adopts per half) instead of deleting it; the O1 note logged once on a failing first apply (record a note marker or the console fingerprint without making the next pass non-first); the N3 sibling (a "restored" pass heals a versioning alarm only when versioning is managed). Accept: acknowledge race (fail-safe, seconds), console kind healing to restored (IDs kept, design), wall-clock TTL — cost if wrong: an extra alarm the owner acknowledges again.
- accept — it can only reproduce a state the owner typed a confirmation for, within 6 h (same class as the accepted N5 trade-off); a 6th call would exhaust the 240 s check budget — cost if wrong: an attacker's suspend that matches the owner's own confirmed suspend goes unalarmed for that window. Mention in the final summary.
- fix round 1 = I1–I3 + Minors (accurate combined copy, reuse _old_version_words on the job page, per-type unsupported wording, wizard copy conditional on managed/unsupported + the real undo days, keep-rule label with days, reject count_days for non-Plain-copy, wrong typed name re-renders the preview with the error like the S3 screen, carry run_now through confirm, drop the duplicate crontab render, the listed tests). Accept: Refresh now inside the wizard preview leaving the page (link to the screen) — cost if wrong: the owner re-enters one change.
- park for the final fix wave: estimate_io._job_inputs treats an unreadable history setting fail-safe (like folders_for) instead of raising — cost if wrong: a hand-edited invalid job breaks cost pages until fixed.
- never let this break a put — detect CLI support once per process (the put-bucket-lifecycle-configuration help/usage lists the flag); when unsupported, never send it (S3 applies its default: objects under 128 KB aren't moved — the benign direction) and add an owner-words detail line when a tier is set ("small files stay where they are"); backlog: newer aws-cli in the image; Task 19 smoke test checks the box's CLI. Folded into Task 18a's review → fix round — cost if wrong: a console-chosen small-object setting gets reset to S3's default.
- fix round 1 = the CLI support ruling (#9 flag only when supported; owner-words detail) + M1, M2, M3 (moved counts only versions not already in an equal-or-colder class by the old tier; a tier not colder than the folder's upload class is refused as a form error / dropped as a no-op), M4 (owner words via vocab's tier label; comment fixed), M5 (tier_keeps_less: a tier with T ≥ expiry days and no newest-N counts as none; a transition with NewerNoncurrentVersions "moves less"), M7 (guards + docstrings name read_lifecycle; remove read_rules if nothing else uses it), M8 (record the #9 value in live.json via save_live), M9 (owner words; the two tests). M6 → carry into 18b's copy. Park: module split (final review).
- park the nit for the final fix wave (run the help probe with no credentials in its env; fix the wording) — cost if wrong: none.
- fix round 1 = I1 (owner-word names via lifecycle._storage_words in damage_notes and the select options; a regression test that the preview/editor HTML contains no bare storage-class constant), I2, I3, both Minors' tests, and one line of M6 copy in the preview when a single row both keeps more (expiry) and adds/moves a tier earlier ("This row waits as a whole — the longer history applies once you confirm the tier"). — cost if wrong: none.
- ONE final fix wave (opus) = I1–I4, I5's notification part (+ document the console-rule residual in spec/README), M1–M12, and the parked items (T10 minors 1–4, T14 screen() recompute, T17 estimate_io fail-safe, T18a help probe without creds). Not neutralizing console rules — spec: console rules are never modified — the optional "treat a new current-expiring console rule as tamper" goes to the owner as a question. lifecycle.py split → backlog after deploy. _human_bytes duplicate → backlog. — cost if wrong: a larger fix diff to re-review.
- M6 follows spec §10 literally — the undo window (storage.json, default 30) feeds the model's existing S3-noncurrent input (versioning_retention_days) for Snapshot/File history, never added to the job's own days; at default settings every estimate must equal e8a176e's exactly (revert the 3 default-path expectation changes; keep the M6 note). One targeted fix (sonnet) = M6 + the two Minors (console-cap after error/unsupported/not-checked; autouse fixture neutralizing notifications in every test). Park the two out-of-scope items in the backlog — cost if wrong: File history estimates exclude the app-kept days' extra noncurrent tail (as before this feature).
- residuals parked to BACKLOG — the undo window can't be priced for "days" policies without changing the frozen model (it reaches only tiered/count/keep_all comparison curves; spec §10 partially met); job_history/impact/wizard "waiting" from the fail-safe reading when storage.json is unreadable (display only, nothing written); wizard _undo_days uses the base folder for a dedicated job (non-default settings only); lifecycle.py split (pure move after deploy); _human_bytes duplicate; newer aws-cli in the image; teardown admin-only — cost if wrong: display-only inaccuracies until done.
- fix before deploy — a settle window (SETTLE_S = 15 min) after the app's own write: a live half equal to the state BEFORE that write is "S3 still settling": no alarm, no write, journal kept, state ok with an owner-words detail; anything else is judged as now. Store the pre-write state with every recorded write (applied.json `previous`) and in the journal. Probe the #9 flag via `--generate-cli-skeleton input`. Harness S7/S8 verification reads use the settle loop. Then the owner re-runs the smoke test. — cost if wrong: tamper detection of an attacker who restores the exact pre-write state is delayed ≤ 15 min + one check.
- settle fix round 2 = I1, I2, M3, and M4 if it stays small (else keep as documented trade-off) — cost if wrong: none.
- accept the M4 mixed-case residual (needs an errored-but-landed put AND a stale read AND a mixed change; the owner re-confirms; never an alarm or data beyond what the owner confirmed) — cost if wrong: the owner confirms twice.
